This is a basic API ready to consume POST requests from lua scripts in Minecraft using the Computer Craft mod.

The idea is that there would be a front end that gets updated often that would display reactor or farm data.

This way users don't have to wait 5 minutes for the JVM to boot up modded minecraft to see if their stuff is working.
